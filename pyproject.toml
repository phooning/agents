[project]
name = "agents"
version = "0.1.7"
description = "Local multi-agent pipeline for software engineering."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "basedpyright>=1.39.3",
  "crewai",
  "langchain-openai",
  "pydantic",
]

[project.scripts]
agents = "agents_pk.main:main"
